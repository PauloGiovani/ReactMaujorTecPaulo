import React from "react";
import Home from "./Home";

// Aqui serão inseridos os componentes da Home Page do site
const Principal = () => {
    return (
        <main className="principal">
            <Home />
        </main>
    );
};

export default Principal;